## 1. Design System & Basic Setup

- [x] 1.1 Create/update CSS variables in `src/index.css` for the premium dark/sleek theme (backgrounds, glassmorphic styling, neon colors, typography scales).
- [x] 1.2 Update `src/App.jsx` to clean up old components and set up standard page layout sections.

## 2. Branding, Header, & Footer

- [x] 2.1 Update `src/components/Header.jsx` to display the `tbats.dev` logo, sleek navigation links, and a highlighted CTA button ("Get a Quote").
- [x] 2.2 Update `src/components/Footer.jsx` with agency contact info, social links, and a concise "About TBATS" description.

## 3. Hero & Testimonials Sections

- [x] 3.1 Redesign `src/components/Hero.jsx` to show a visually striking, business-focused title, value proposition, CTA buttons, and background glow effects.
- [x] 3.2 Add a testimonials container/grid containing at least 3 reviews with client names, business roles, and mock star ratings.

## 4. Client Journey Timeline

- [x] 4.1 Create a new component `src/components/ClientTimeline.jsx` showing the 5-phase website creation lifecycle (Specs -> Design -> Develop -> Optimize -> Launch).
- [x] 4.2 Add clean animations or interactive hover triggers to highlight active timeline steps.

## 5. Interactive Layout & Pricing Estimator

- [x] 5.1 Create `src/components/InteractiveEstimator.jsx` containing checkboxes/inputs for design options (Pages, E-Commerce, Custom Animation, CMS/Blog, SEO package).
- [x] 5.2 Implement the live price logic in Philippine Pesos (PHP) with a base rate (e.g. ₱29,000) plus addons.
- [x] 5.3 Implement a dynamic visual mock browser wireframe that updates its visual modules (header, layout blocks, checkout cart, animations) based on selected estimator options.

## 6. Consultation Request Form

- [x] 6.1 Create `src/components/ConsultationForm.jsx` with input fields: Name, Email/Phone, Project description, budget tier, and preferred scheduling window.
- [x] 6.2 Implement basic email/phone validation and a simulated mock success toast/modal showing "Consultation Requested successfully!".

## 7. Responsive Polish & QA

- [x] 7.1 Verify that the entire layout is responsive on mobile screen sizes.
- [x] 7.2 Run linter and ensure Vite build passes successfully.
