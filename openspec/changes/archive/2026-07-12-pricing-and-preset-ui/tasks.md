## 1. Pricing Updates

- [x] 1.1 Update Starter tier package price to `₱15,000` and Growth tier package price to `₱30,000` in `src/components/InteractiveEstimator.jsx`

## 2. Redesigned Preset Selectors

- [x] 2.1 Add `swatches` color arrays to each entry in the `PRESETS` definition inside `src/components/Portfolio.jsx`
- [x] 2.2 Refactor the preset selector rendering in `src/components/Portfolio.jsx` to stack vertically
- [x] 2.3 Implement color swatch dot elements and styling badges inside each stacked preset card
- [x] 2.4 Verify that changing presets updates the deployment specs preview box correctly
- [x] 2.5 Run build checks (`npm run build`) to ensure zero bundling errors
