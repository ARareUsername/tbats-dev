## 1. Timeline and Location Refactoring

- [x] 1.1 Redesign `src/components/ClientTimeline.jsx` to show a 5-step process timeline with `id="process"`.
- [x] 1.2 Update the agency location to "Pampanga, Philippines" in `src/components/Footer.jsx`.

## 2. Aesthetics and Navigation

- [x] 2.1 Refactor `body::before` background noise filter in `src/index.css` to render a native high-resolution grainy pattern.
- [x] 2.2 Add `scroll-behavior: smooth` to the html styling in `src/index.css`.
- [x] 2.3 Fix anchors and target IDs in `src/components/Hero.jsx` and other files to enable smooth scroll-to-section.
