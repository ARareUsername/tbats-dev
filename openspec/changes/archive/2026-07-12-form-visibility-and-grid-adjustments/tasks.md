## 1. Contact Form Refactoring

- [x] 1.1 Refactor form card background to solid `var(--bg-dark-end)` and update all text input, select, and textarea elements in `ConsultationForm.jsx` to use dynamic `color: 'var(--text-primary)'` and solid `background: 'var(--bg-input)'` styling

## 2. Styling and Badges Polishing

- [x] 2.1 Update `ServicesList.jsx` to replace hardcoded transparent white heading font colors with theme-adaptive `var(--text-primary)` text styles at `0.4` opacity
- [x] 2.2 Update `Team.jsx` focus skill tag background elements to use `var(--border-color)` instead of hardcoded transparent white backgrounds

## 3. CSS Styles & Validation

- [x] 3.1 Define solid `--bg-input` theme variables under `:root` and `body.theme-light` inside `index.css`
- [x] 3.2 Run linter checks and local builds to verify code correctness and visual styling output
